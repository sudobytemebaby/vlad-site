---
name: use_bun
description: User prefers bun over npm for package management
type: feedback
---

Use `bun` instead of `npm` for installing packages and running scripts.

**Why:** User preference — corrected me when I used npm.
**How to apply:** Always use `bun add`, `bun install`, `bun run` etc. instead of npm equivalents.
